import 'packagefluttermaterial.dart';
 
class HelloWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Text(HelloW, style TextStyle(fontSize 24));
  }
}

